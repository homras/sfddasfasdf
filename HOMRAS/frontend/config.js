const config = {
    API_BASE_URL: process.env.API_BASE_URL || 'https://homras.onrender.com/api',
    APP_NAME: 'HOMRAS',
    DEFAULT_LANGUAGE: 'ka',
    SUPPORTED_LANGUAGES: ['ka', 'en', 'ru']
};


export default config;
